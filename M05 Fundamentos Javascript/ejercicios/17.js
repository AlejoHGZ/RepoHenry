function esPar(num) {
  // Retorna true si "num" es par.
  // De lo contrario, retorna false.
  // Tu código:

  let numero;

  numero = num / 2 == true || num == false;

  return numero;

}
var Par = 3;
console.log(esPar(Par));

