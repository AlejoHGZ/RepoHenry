function obtenerAreaRectangulo(alto, ancho) {
  // Retornar el área de un rectángulo teniendo su alto y ancho.
  // Tu código:

  let area = alto * ancho;
  return area;
}

console.log("El area del rectangulo es: " + obtenerAreaRectangulo(15, 5));
